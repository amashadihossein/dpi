# This won't work if test bucket repo is not set up

test_that("dp_make_params with s3 board", {

  # repo_link <- "https://github.com/camorosi/dp_cars_test"
  # dpparams <- dp_make_params(repo_link)
  #
  # expect_is(dpparams, "list")
  # expect_s3_class(dpparams$board_params, "data.frame")
  # expect_s3_class(dpparams$board_params, "s3_board")
  # expect_named(dpparams, c("board_params", "creds", "data_name", "latest_pin_version"))
  #
  # expect_s3_class(dpparams$creds, "data.frame")
  # expect_s3_class(dpparams$creds, "aws.creds")
  # expect_named(creds, c("profile_name", "key", "secret"))
})


